{
  "apiVersion": "qos-binding/v1",
  "kind": "ConstraintSet",
  "metadata": {
    "name": "pautasso2009_restful_ecommerce_constraints"
  },
  "spec": {
    "constraints": {
      "bind_shop_mono_provider": {
        "assert": "tasks.t_create_order.provider == tasks.t_add_item.provider && tasks.t_create_order.provider == tasks.t_remove_item.provider && tasks.t_create_order.provider == tasks.t_get_amount.provider && tasks.t_create_order.provider == tasks.t_checkout.provider && tasks.t_create_order.provider == tasks.t_list_confirmed.provider && tasks.t_create_order.provider == tasks.t_ship_order.provider && tasks.t_create_order.provider == tasks.t_update_shipment.provider && tasks.t_create_order.provider == tasks.t_cancel_order.provider",
        "enforcement": "hard"
      },
      "bind_catalog_mono_provider": {
        "assert": "tasks.t_request_quote.provider == tasks.t_get_quote.provider",
        "enforcement": "hard"
      },
      "bind_payment_mono_provider": {
        "assert": "tasks.t_charge_payment.provider == tasks.t_refund_payment.provider",
        "enforcement": "hard"
      }
    }
  }
}
